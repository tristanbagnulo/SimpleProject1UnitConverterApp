package com.example.simple_project_1_unit_converter_app;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class VolumeConversionActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_volume_conversion);
    }

    // 1. Accept user input into any one field. When user
    // clicks "translate" button do the following.
    // 2. Identify which unit was input.
        // 2.1. Perform translation to other units depending
        // on which was input.
        // 2.2 Assign output value of each unit to variables.
    // 3. Update all input fields to display results
}